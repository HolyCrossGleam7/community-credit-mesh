import sys\nfrom PyQt6.QtWidgets import QApplication, QMainWindow, QLabel, QLineEdit, QPushButton, QVBoxLayout, QWidget\n\nclass LoginWindow(QMainWindow):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle('Login')\n        self.setGeometry(100, 100, 280, 80)\n        self.initUI()\n\n    def initUI(self):\n        self.label_username = QLabel('Username:')\n        self.input_username = QLineEdit()\n        self.label_password = QLabel('Password:')\n        self.input_password = QLineEdit()\n        self.input_password.setEchoMode(QLineEdit.EchoMode.Password)\n        self.button_login = QPushButton('Login')\n        self.button_login.clicked.connect(self.handle_login)\n        \n        layout = QVBoxLayout()\n        layout.addWidget(self.label_username)\n        layout.addWidget(self.input_username)\n        layout.addWidget(self.label_password)\n        layout.addWidget(self.input_password)\n        layout.addWidget(self.button_login)\n\n        container = QWidget()\n        container.setLayout(layout)\n        self.setCentralWidget(container)\n\n    def handle_login(self):\n        username = self.input_username.text()\n        password = self.input_password.text()\n        # Handle login logic here\n        print(f'Username: {username}, Password: {password}')\n\nclass WalletView(QWidget):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle('Wallet View')\n        self.initUI()\n\n    def initUI(self):\n        layout = QVBoxLayout()\n        self.label_balance = QLabel('Your balance: $0.00')\n        layout.addWidget(self.label_balance)\n        self.setLayout(layout)\n\nclass TransactionInterface(QWidget):\n    def __init__(self):\n        super().__init__()\n        self.setWindowTitle('Transaction Interface')\n        self.initUI()\n\n    def initUI(self):\n        layout = QVBoxLayout()\n        self.label_transaction = QLabel('Transaction Details:')\n        layout.addWidget(self.label_transaction)\n        self.setLayout(layout)\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    login_window = LoginWindow()\n    login_window.show()\n    sys.exit(app.exec())